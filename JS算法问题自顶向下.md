# 算法题元操作
## 迭代

1. **迭代是什么：** 通过循环（`for`, `while`）重复执行一段代码，通常伴随着一个或多个状态变量的更新。
2. **JS实现：** 通过`for`、`while`、`for...of`等遍历对象，`for...in`遍历对象属性等
3. **使用场景：** 遍历线性结构

## 递归
1. **递归是什么：** 函数本身所要解决的问题和函数的子结构要解决的问题相同，此时函数可以不断调用自身，并以某个条件作为截止，直到返回结果的解决方式
2. **JS实现：** 函数调用自身，设置终止条件
3. **使用场景：** 函数结构层级嵌套，可以反复调用己身，处理嵌套的数据结构等

## 数理逻辑运算
1. **什么是数理逻辑运算**：包括数学运算、比较、位运算、交换、求余等
2. **JS实现：** ES操作
3. **使用场景：** 数据处理

## 对象引用
1. **JS对象引用机制：** JS有一个特性，即其中所有数组，函数等都是通过对象类型创建的，其本质都是对象类型，也就是通过地址存储的
2. **JS实现：** 使用对象进行直接赋值时会产生引用的绑定
3. **使用场景：** 共享对象数据

## 集合
1. **什么是集合：** 集合即Set数据类型，具有保证数据唯一性的特点
2. **JS实现：** new Set()
3. **使用场景：** 数据去重

## 映射
1. **什么是映射：** 映射指将某几个数据和其他数据相绑定，建立映射关系，通常使用map进行维护
2. **JS实现：** new Map()
3. **使用场景：** 存储需要被映射绑定的数据

# 核心解题技巧
## 模拟
1. **模拟是什么：** 不依赖算法技巧，严格按照题目描述顺序执行
2. **使用场景：** 流程清晰、步骤固定的问题

## 枚举
1. **枚举：** 尝试遍历所有数据，找到满足条件的解
2. **使用场景：** 数据集足够小的时候、没有明显规律可寻的问题

## 滑动窗口
1. **滑动窗口：** 通过维护一个动态窗口，不断移动窗口左边界和右边界并进行状态更新，从而找到结果
2. **使用场景：** 求满足条件的连续子数组、子串问题

## 双指针
1. **双指针是什么：** 使用两个指针以不同策略遍历整个数据的数据结构
2. **使用场景：** 二分查找、反转链表、寻找满足条件的两个值

## 分治
1. **分治是什么：** 将一个大问题递归分为许多小问题，通过解决这些子问题并合并结果从而解决大问题
2. **使用场景：** 问题为多个相同结构问题的嵌套，子问题相互独立，快排、归并排序等

## 回溯
1. **回溯是什么：** 回溯是指通过一种反复试探和撤销来寻找可行解的方法，常用于DFS路径回归等路径探索场景
2. **使用场景：** 需要设定方向并进行递归试探的场景

## 动态规划
1. **动态规划是什么：** dp指将原问题分解为互相重叠的子问题，并存储子问题的解，从而高效解决问题，通常包含一个转移方程
2. **使用场景：** 求最优解、问题可以被分解为子问题，且子问题可以被重复计算，拥有最优子结构


# 数据结构

## 链表

## 二叉树
